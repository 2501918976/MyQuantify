<Page x:Class="MyQuantifyApp.Views.FocusView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyQuantifyApp.Views"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1200"
      Title="FocusView"
      Background="#F8F9FA">

    <Grid>
        <Grid.RowDefinitions>
            <!-- 上部分：专注区域和设置区域，占 600 高度 -->
            <RowDefinition Height="600"/>
            <!-- 下部分：进度条区域，占 100 高度 -->
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <!-- ======================================================= -->
        <!-- 上部分容器 (Grid.Row="0")：600 高度，分为左右两列 -->
        <!-- ======================================================= -->
        <Border Grid.Row="0" Margin="10" Background="White" CornerRadius="8" BorderBrush="#E0E6ED" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <!-- 左边：沙漏/闹钟技术区域 (使用 2* 比例分配更多空间给主功能) -->
                    <ColumnDefinition Width="2*"/>
                    <!-- 右边：设置选项区域 (使用 1* 比例) -->
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <!-- 左侧内容：沙漏/闹钟技术 (Grid.Column="0") -->
                <Border Grid.Column="0" 
                        Margin="10" 
                        BorderBrush="#E0E6ED" 
                        BorderThickness="0,0,1,0" 
                        Padding="10">
                    <TextBlock Text="[专注计时器区域：沙漏/闹钟技术实现]"
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center" 
                               FontSize="20"
                               Foreground="#3498DB"
                               FontWeight="SemiBold"/>
                </Border>

                <!-- 右侧内容：设置选项 (Grid.Column="1") -->
                <Border Grid.Column="1" Margin="10" Padding="10">
                    <TextBlock Text="[设置选项区域：专注时长/休息时间/目标设置]"
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center" 
                               FontSize="16"
                               Foreground="#2C3E50"
                               TextWrapping="Wrap"
                               TextAlignment="Center"/>
                </Border>
            </Grid>
        </Border>

        <!-- ======================================================= -->
        <!-- 下部分容器 (Grid.Row="1")：100 高度，进度条区域 -->
        <!-- ======================================================= -->
        <Border Grid.Row="1" Margin="10" Background="White" CornerRadius="8" BorderBrush="#E0E6ED" BorderThickness="1">
            <TextBlock Text="[底部进度条/专注状态展示区域]"
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       FontSize="18"
                       Foreground="#27AE60"/>
        </Border>
    </Grid>
</Page>
